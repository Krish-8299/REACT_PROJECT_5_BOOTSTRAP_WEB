const Start = ({ text }) => {
  return (
    <div className="bg-dark text-white py-2">
      <div className="container">
        <p className="text-center m-0 small">{text}</p>
      </div>
    </div>
  );
};

export default Start;
